name: Build Obelisk Util

on:
  push:
    branches:
      - "development"
    paths:
      - "obelisk-util/**"
  pull_request:
    branches:
      - "development"
    paths:
      - "obelisk-util/**"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
      - name: Build with Maven
        run: mvn -B package --file obelisk-util/pom.xml